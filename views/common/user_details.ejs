<p id="username">Username: <%= data.username %></p>
<p>Display Name: <%= data.displayName %></p>
<% if (data.author) { %>
    <p id="author-perms-text">This user is an author.</p> 
<% } %>

<% if(data.bio) { %>
    <p id="bio"><i>"<%= data.bio %>"</i></p>
<% } else { %>
    <p>This user has no bio.</p>
<% } %>